{
  "mcpServers": {
    "plane": {
      "command": "npx",
      "args": ["-y", "@makeplane/plane-mcp-server", "stdio"],
      "env": {
        "PLANE_API_KEY": "$PLANE_API_KEY",
        "PLANE_WORKSPACE_SLUG": "woam",
        "PLANE_BASE_URL": "https://api.plane.so"
      }
    }
  }
}
